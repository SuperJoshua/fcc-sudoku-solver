"use strict"

const SS = require("../controllers/sudoku-solver.js")

module.exports = function (app) {
  app.route("/api/check")
    .post((req, res) => {

    })
    
  app.route("/api/solve")
    .post((req, res) => {

    })
}
